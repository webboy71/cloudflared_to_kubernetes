apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test
  namespace: default
spec:
  podSelector: 
    matchLabels:
      app: httpbin
  policyTypes:
  - Ingress
  ingress:
  - from:
    #- ipBlock:
    #    cidr: 10.0.0.0/8
    - podSelector: 
        matchLabels:
          app: httpbin